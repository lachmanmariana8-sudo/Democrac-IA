# PyTorch CPU-only wheels.
# Railway corre sin GPU: sin este pin, `sentence-transformers` arrastra
# `torch` + todo el stack NVIDIA CUDA (~2.5GB de nvidia-cu*, triton, etc.).
# Con el pin +cpu la imagen baja de 3GB a ~500MB y el build es mucho más rápido.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu

langgraph>=0.2.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pandas>=2.0.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
# microsoft/markitdown — conversión HTML/PDF/DOCX → Markdown limpio
markitdown>=0.0.1
# xhtml2pdf SE QUITO el 2-may-2026 porque su dependencia transitiva pycairo
# (via svglib > rlpycairo) requiere libcairo headers que Nixpacks no provee
# de forma confiable. Cada deploy intentado con xhtml2pdf fallo en build.
# PDF on-demand desde SQLite responde 503 con mensaje claro hasta que
# encontremos un PDF builder pure-python sin pycairo (reportlab nativo,
# pyfpdf, etc.).
