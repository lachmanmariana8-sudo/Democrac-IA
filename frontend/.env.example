# DEMOCRAC.IA Frontend — Variables de entorno
# En desarrollo local: no se necesita (usa localhost:8000 por defecto)
# En producción (Netlify): configurar en el panel Environment Variables

# URL del backend en producción (Railway)
# Formato: https://api.democracia.ar  (o el dominio Railway antes de configurar el custom domain)
VITE_API_BASE=https://api.democracia.ar
