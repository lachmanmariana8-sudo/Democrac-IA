[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirige todas las rutas al index.html (SPA)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
